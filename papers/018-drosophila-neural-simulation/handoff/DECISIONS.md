# ARIS4C018 · Decision log

This file is a compact handoff summary. The full append-only scientific decision history is `../DECISIONS.md`.

## 2026-09-19 · continuity standard adopted

Git is the cross-session / cross-device / cross-agent canonical surface. The handoff package summarizes canonical scientific files; it does not replace them.

## 2026-09-21 · 018 remains the exploration mother project

Do not fork merely because a prototype runs. Fork only when a focused paper question or independently useful software product has a stable boundary.

## 2026-09-21 · distinguish BIO / DECODER / BODY

The retinal/flyvis neural layer is biologically/connectome-constrained.

The z-score → object mask → turning-bias → two-value descending-drive mapping is engineered.

Do not describe the full chain as a reconstructed biological visual-to-motor pathway.

## 2026-09-21 · legacy reproduction before current migration

The published legacy advanced-vision path was reproduced first. Only after that was the interface migrated to FlyGym 2.x.

This prevents migration debugging from being confused with reproduction.

## 2026-09-21 · migration engineering closed only after trace parity

FlyGym 1.x→2.x migration was considered closed only after:

- current BIO neural interface PASS;
- current full chain PASS under strict pre-frozen conditions;
- current synchronized trace committed;
- semantic trace parity PASS;
- one replay UI could load either provenance.

## 2026-09-21 · Pilot 4 is diagnostic, not a version ranking

The matched static-target comparison found a first-frame decoder divergence.

Do not choose a version winner or create an equivalence threshold after observing the result.

Use the pre-frozen R1→R5 localization ladder.

## 2026-09-24 · R1 and R2 excluded

R1 Retina geometry/order is byte-identical.

R2 frozen retinal vectors, mapped inputs and pinned flyvis neural responses are exactly equal across the two stacks.

Therefore the unresolved divergence moves to the actual rendered first-frame sensory vector or temporal-state initialization.

## 2026-09-24 · R2.5 stays frozen despite Actions failure

R2.5 code and workflow are committed.

The latest R2.5 job and repository-wide automation jobs fail before any step executes.

Treat this as Actions/runner infrastructure blocked, not a scientific failure. Re-run the same committed R2.5 gate first when execution recovers.

## Deletion-readiness decision

The source chat is **not** a required research artifact.

A new executor should continue from:

1. `AGENT_HANDOFF.md`
2. `STATUS.md`
3. `TODO.md`
4. `../PILOT4_R2_5_RENDERER_GATE.md`
5. `../PILOT4_R2_RESULT.md`

Do not ask the user to reconstruct this conversation.
